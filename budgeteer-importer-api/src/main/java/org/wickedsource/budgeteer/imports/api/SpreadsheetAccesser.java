package org.wickedsource.budgeteer.imports.api;

//import org.apache.poi.ss.usermodel.*;

public class SpreadsheetAccesser {
    /*
    public static boolean cellContentIsImportable(Row row, int cellNumber, CellContentValidator validator) {
        Cell cell = row.getCell(cellNumber);
        if (cell != null) {
            String cellValue = cell.getStringCellValue();
            if (cellValue != null) {
                return validator.isValid(cellValue);
            }
        }
        return false;
    }
    */
}
